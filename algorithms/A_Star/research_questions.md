# Research Questions

## Fundamental Concepts

- What problem does A* solve?
- What is a heuristic?
- Why does A* use both g(n) and h(n)?
- What does f(n) represent?
- Why is f(n) = g(n) + h(n)?

---

## Heuristics

- What makes a heuristic admissible?
- What makes a heuristic consistent?
- Why does an admissible heuristic guarantee the shortest path?
- What happens if the heuristic overestimates the remaining distance?
- What happens if the heuristic is always zero?
- How is Manhattan Distance calculated?
- How is Euclidean Distance calculated?
- When should Manhattan Distance be preferred over Euclidean Distance?

---

## Algorithm

- Why does A* usually explore fewer nodes than Dijkstra?
- Why does A* stop once the goal is reached?
- Why is a priority queue required?
- Why are g-score and f-score stored separately?
- How does A* update a better path?

---

## Comparisons

- How does A* differ from Dijkstra?
- How does A* differ from BFS?
- How does A* differ from DFS?
- When should A* be preferred over Dijkstra?
- When does A* become equivalent to Dijkstra?

---

## Applications

- How is A* used in GPS navigation?
- How is A* used in robotics?
- How do video games use A* for pathfinding?
- How do autonomous vehicles use A*?

---

## Advanced Exploration

- Can A* use multiple heuristics?
- How can heuristics be learned automatically?
- What happens if obstacles change while A* is running?
- How does A* behave on very large graphs?
- What optimizations exist for A*?
- How do weighted A* and Anytime A* differ from standard A*?
